Please generate the exact shell commands to create a new React + TypeScript app using Vite called System-Inventory-Management-System. 
I want npm as the package manager. 
Then include the commands to install dependencies and run the dev server.